databaseChangeLog:
  - changeSet:
      id: 1
      author: pivotal
      changes:
        - createTable:
            tableName: match
            columns:
              - column:
                  name: id
                  type: varchar(255)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: match_request1id
                  type: varchar(255)
                  constraints:
                    nullable: false
              - column:
                  name: match_request2id
                  type: varchar(255)
                  constraints:
                    nullable: false
